<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>JUST – Online Mathematics Exam</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;900&family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;1,400&display=swap" rel="stylesheet">
<style>
:root {
  --navy:#0d1b3e; --blue:#1a3a6e; --gold:#c8a84b; --gold2:#e8c96a;
  --white:#fafaf8; --gray:#f0eff5; --text:#1a1a2e; --muted:#6b7280;
  --correct:#10b981; --wrong:#ef4444; --border:#d1d9ef;
}
*{margin:0;padding:0;box-sizing:border-box;}
body{font-family:'DM Sans',sans-serif;background:var(--navy);min-height:100vh;color:var(--text);}

/* SCREENS */
.screen{display:none;min-height:100vh;}
.screen.active{display:flex;}

/* ═══ WELCOME ═══ */
#s-welcome{align-items:center;justify-content:center;padding:30px 16px;
  background:radial-gradient(ellipse at 30% 20%,rgba(200,168,75,.12),transparent 60%),var(--navy);}
.wcard{background:var(--white);max-width:520px;width:100%;
  box-shadow:0 40px 100px rgba(0,0,0,.55),0 0 0 1px rgba(200,168,75,.25);
  animation:rise .6s cubic-bezier(.16,1,.3,1) both;}
@keyframes rise{from{opacity:0;transform:translateY(36px)}to{opacity:1;transform:none}}
.whead{background:var(--blue);padding:30px 34px 24px;position:relative;overflow:hidden;}
.whead::after{content:'';position:absolute;bottom:0;left:0;right:0;height:3px;
  background:linear-gradient(90deg,var(--gold),var(--gold2),var(--gold));}
.logo-row{display:flex;align-items:center;gap:12px;margin-bottom:18px;}
.logo-sq{width:42px;height:42px;border:2px solid var(--gold);display:flex;align-items:center;
  justify-content:center;font-family:'Playfair Display',serif;font-size:15px;font-weight:700;color:var(--gold);}
.uni-info{color:rgba(255,255,255,.72);font-size:11px;line-height:1.55;letter-spacing:.04em;}
.whead h1{font-family:'Playfair Display',serif;font-size:24px;color:#fff;line-height:1.25;}
.gold-rule{width:46px;height:2px;background:var(--gold);margin:10px 0 7px;}
.whead p{color:rgba(255,255,255,.6);font-size:12.5px;letter-spacing:.03em;}
.wbody{padding:30px 34px;}
.fl{display:block;font-size:10px;font-weight:600;letter-spacing:.12em;text-transform:uppercase;
  color:var(--blue);margin-bottom:6px;}
.fi{width:100%;border:none;border-bottom:2px solid var(--border);background:transparent;
  padding:8px 4px;font-family:'DM Sans',sans-serif;font-size:15px;color:var(--text);
  outline:none;transition:border-color .2s;}
.fi:focus{border-bottom-color:var(--gold);}
.fi::placeholder{color:#c0bfc8;font-size:13.5px;}
.fg{margin-bottom:20px;}
.info-strip{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;
  background:var(--border);border:1px solid var(--border);margin:20px 0 26px;}
.ic{background:var(--gray);padding:11px 10px;text-align:center;}
.ic-l{font-size:9px;font-weight:600;letter-spacing:.1em;text-transform:uppercase;color:var(--muted);}
.ic-v{font-size:13px;font-weight:600;color:var(--navy);margin-top:3px;}
.btn-go{width:100%;background:var(--blue);color:#fff;border:none;padding:15px;cursor:pointer;
  font-family:'DM Sans',sans-serif;font-size:13px;font-weight:600;letter-spacing:.08em;
  text-transform:uppercase;position:relative;overflow:hidden;transition:background .2s;}
.btn-go:hover{background:var(--navy);}
.btn-go::after{content:'';position:absolute;bottom:0;left:0;right:0;height:3px;
  background:linear-gradient(90deg,var(--gold),var(--gold2));}
.credit{text-align:center;font-size:11px;color:var(--muted);padding:14px 0 0;}
.credit strong{color:var(--blue);}

/* ═══ EXAM ═══ */
#s-exam{flex-direction:column;background:linear-gradient(155deg,#f5f3ee,#edf0f8);}
.topbar{background:var(--blue);height:54px;display:flex;align-items:center;
  padding:0 24px;gap:16px;position:sticky;top:0;z-index:50;
  box-shadow:0 2px 16px rgba(0,0,0,.35);}
.tb-logo{font-family:'Playfair Display',serif;font-size:13px;font-weight:700;
  color:var(--gold);border:1px solid rgba(200,168,75,.4);padding:4px 10px;letter-spacing:1px;}
.tb-mid{flex:1;text-align:center;color:rgba(255,255,255,.7);font-size:12px;}
.tb-mid strong{color:#fff;}
.tb-timer{background:rgba(0,0,0,.3);border:1px solid var(--gold);
  padding:5px 14px;color:var(--gold);font-size:14px;font-weight:600;letter-spacing:.05em;
  font-variant-numeric:tabular-nums;}
.tb-timer.warn{color:#f87171;border-color:#f87171;animation:pulse 1s infinite;}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.6}}

.exam-wrap{flex:1;padding:28px 20px;max-width:760px;margin:0 auto;width:100%;}

/* progress */
.progress-row{display:flex;align-items:center;gap:14px;margin-bottom:24px;}
.prog-bar{flex:1;height:5px;background:#dde3ef;border-radius:3px;overflow:hidden;}
.prog-fill{height:100%;background:linear-gradient(90deg,var(--blue),var(--gold));
  border-radius:3px;transition:width .4s ease;}
.prog-txt{font-size:12px;color:var(--muted);flex-shrink:0;}

/* question card */
.qcard{background:#fff;border-radius:2px;
  box-shadow:0 4px 24px rgba(0,0,0,.08),0 0 0 1px rgba(200,168,75,.12);
  overflow:hidden;animation:qin .3s ease both;}
@keyframes qin{from{opacity:0;transform:translateX(20px)}to{opacity:1;transform:none}}
.qcard-head{background:var(--blue);padding:16px 24px;display:flex;align-items:center;gap:14px;}
.q-badge{background:var(--gold);color:var(--navy);
  font-weight:700;font-size:11px;padding:4px 10px;letter-spacing:.06em;}
.q-section{color:rgba(255,255,255,.65);font-size:12px;flex:1;}
.q-marks-tag{color:var(--gold);font-size:12px;font-weight:600;}
.qcard-body{padding:26px 24px 20px;}
.q-text{font-size:15px;line-height:1.7;color:var(--text);margin-bottom:20px;}
.q-text .formula{
  display:inline-block;background:#f0f4fb;border-left:3px solid var(--blue);
  padding:6px 14px;font-family:'Courier New',monospace;font-size:13.5px;
  margin:6px 0;color:var(--navy);}

/* options */
.options{display:flex;flex-direction:column;gap:10px;}
.opt{display:flex;align-items:center;gap:14px;padding:13px 16px;
  border:1.5px solid var(--border);background:#fafaf8;cursor:pointer;
  transition:all .18s;position:relative;user-select:none;}
.opt:hover{border-color:var(--blue);background:#f0f4ff;}
.opt.selected{border-color:var(--blue);background:#eef2ff;}
.opt.correct{border-color:var(--correct)!important;background:#f0fdf8!important;}
.opt.wrong{border-color:var(--wrong)!important;background:#fef2f2!important;}
.opt-key{width:28px;height:28px;border:1.5px solid var(--border);
  display:flex;align-items:center;justify-content:center;
  font-size:12px;font-weight:600;color:var(--muted);flex-shrink:0;transition:all .18s;}
.opt.selected .opt-key{background:var(--blue);border-color:var(--blue);color:#fff;}
.opt.correct .opt-key{background:var(--correct);border-color:var(--correct);color:#fff;}
.opt.wrong .opt-key{background:var(--wrong);border-color:var(--wrong);color:#fff;}
.opt-text{font-size:14px;color:var(--text);}
.feedback-tag{margin-left:auto;font-size:11px;font-weight:600;
  padding:3px 9px;flex-shrink:0;}
.feedback-tag.c{background:#d1fae5;color:#065f46;}
.feedback-tag.w{background:#fee2e2;color:#991b1b;}

/* nav */
.nav-row{display:flex;justify-content:space-between;align-items:center;
  margin-top:22px;gap:12px;}
.btn-nav{padding:11px 28px;border:none;cursor:pointer;
  font-family:'DM Sans',sans-serif;font-size:13px;font-weight:600;
  letter-spacing:.06em;text-transform:uppercase;transition:all .18s;}
.btn-prev{background:transparent;border:1.5px solid var(--border);color:var(--muted);}
.btn-prev:hover{border-color:var(--navy);color:var(--navy);}
.btn-next{background:var(--blue);color:#fff;position:relative;overflow:hidden;}
.btn-next:hover{background:var(--navy);}
.btn-next::after{content:'';position:absolute;bottom:0;left:0;right:0;height:2px;
  background:linear-gradient(90deg,var(--gold),var(--gold2));}
.btn-submit{background:var(--gold);color:var(--navy);}
.btn-submit:hover{background:var(--gold2);}

/* answered dots */
.q-dots{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:22px;}
.qdot{width:26px;height:26px;border:1.5px solid var(--border);
  display:flex;align-items:center;justify-content:center;
  font-size:10px;font-weight:600;color:var(--muted);cursor:pointer;
  transition:all .15s;}
.qdot.done{background:var(--blue);border-color:var(--blue);color:#fff;}
.qdot.active{background:var(--gold);border-color:var(--gold);color:var(--navy);}

/* ═══ RESULTS ═══ */
#s-result{flex-direction:column;align-items:center;justify-content:flex-start;
  background:radial-gradient(ellipse at 50% 0%,rgba(200,168,75,.12),transparent 55%),var(--navy);
  padding:40px 20px;}
.rcard{background:var(--white);max-width:600px;width:100%;
  box-shadow:0 40px 80px rgba(0,0,0,.5);
  animation:rise .5s cubic-bezier(.16,1,.3,1) both;}
.rhead{background:var(--blue);padding:28px 34px 22px;position:relative;overflow:hidden;}
.rhead::after{content:'';position:absolute;bottom:0;left:0;right:0;height:3px;
  background:linear-gradient(90deg,var(--gold),var(--gold2),var(--gold));}
.rhead h2{font-family:'Playfair Display',serif;font-size:22px;color:#fff;}
.rhead p{color:rgba(255,255,255,.65);font-size:13px;margin-top:5px;}
.rbody{padding:30px 34px;}
.score-circle{text-align:center;margin-bottom:28px;}
.sc-num{font-family:'Playfair Display',serif;font-size:60px;font-weight:900;
  color:var(--blue);line-height:1;}
.sc-denom{font-size:22px;color:var(--muted);}
.sc-label{font-size:13px;color:var(--muted);margin-top:6px;letter-spacing:.05em;}
.sc-grade{display:inline-block;margin-top:10px;padding:6px 20px;
  font-size:14px;font-weight:700;letter-spacing:.06em;}
.grade-A{background:#d1fae5;color:#065f46;}
.grade-B{background:#dbeafe;color:#1e3a8a;}
.grade-C{background:#fef3c7;color:#92400e;}
.grade-F{background:#fee2e2;color:#991b1b;}

.stats-grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:1px;
  background:var(--border);border:1px solid var(--border);margin-bottom:26px;}
.sg-cell{background:var(--gray);padding:14px;text-align:center;}
.sg-val{font-size:20px;font-weight:700;color:var(--navy);}
.sg-lbl{font-size:10px;text-transform:uppercase;letter-spacing:.1em;color:var(--muted);margin-top:3px;}

/* breakdown */
.breakdown{margin-bottom:20px;}
.bd-title{font-size:11px;font-weight:600;letter-spacing:.1em;text-transform:uppercase;
  color:var(--blue);margin-bottom:10px;}
.bd-item{display:flex;align-items:center;gap:10px;padding:9px 0;
  border-bottom:1px solid #eee;font-size:13px;}
.bd-num{width:22px;height:22px;background:var(--blue);color:#fff;
  display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:700;flex-shrink:0;}
.bd-q{flex:1;color:var(--text);line-height:1.4;}
.bd-tag{font-size:10px;font-weight:700;padding:3px 8px;flex-shrink:0;}
.bd-tag.c{background:#d1fae5;color:#065f46;}
.bd-tag.w{background:#fee2e2;color:#991b1b;}
.bd-ans{font-size:11px;color:var(--muted);margin-top:2px;}

.btn-redo{width:100%;background:var(--blue);color:#fff;border:none;
  padding:14px;cursor:pointer;font-family:'DM Sans',sans-serif;
  font-size:13px;font-weight:600;letter-spacing:.08em;text-transform:uppercase;
  position:relative;overflow:hidden;transition:background .2s;}
.btn-redo:hover{background:var(--navy);}
.btn-redo::after{content:'';position:absolute;bottom:0;left:0;right:0;height:2px;
  background:linear-gradient(90deg,var(--gold),var(--gold2));}
.rcredit{text-align:center;font-size:11px;color:rgba(255,255,255,.4);margin-top:16px;}
.rcredit strong{color:rgba(255,255,255,.65);}
</style>
</head>
<body>

<!-- ══════ WELCOME ══════ -->
<div class="screen active" id="s-welcome">
  <div class="wcard">
    <div class="whead">
      <div class="logo-row">
        <div class="logo-sq">JUST</div>
        <div class="uni-info">Jamhuriya University of Science & Technology<br>Faculty of Computer & Information Technology</div>
      </div>
      <h1>Mathematics Online Exam</h1>
      <div class="gold-rule"></div>
      <p>F1 Arithmetic &nbsp;·&nbsp; F2 Algebra, Powers & Logarithms</p>
    </div>
    <div class="wbody">
      <div class="fg">
        <label class="fl">Student Full Name</label>
        <input class="fi" id="inp-name" type="text" placeholder="Magacaaga oo dhan halkan ku qor…">
      </div>
      <div class="info-strip">
        <div class="ic"><div class="ic-l">Macalin</div><div class="ic-v">Sacad Siciid</div></div>
        <div class="ic"><div class="ic-l">Su'aalo</div><div class="ic-v">20 Questions</div></div>
        <div class="ic"><div class="ic-l">Waqti</div><div class="ic-v">30 Daqiiqo</div></div>
      </div>
      <button class="btn-go" onclick="startExam()">▶ &nbsp; Bilow Imtixaanka</button>
      <div class="credit">Prepared by: <strong>Sacad Siciid</strong> &nbsp;·&nbsp; JUST – Faculty of CIT</div>
    </div>
  </div>
</div>

<!-- ══════ EXAM ══════ -->
<div class="screen" id="s-exam">
  <div class="topbar">
    <div class="tb-logo">JUST</div>
    <div class="tb-mid">Mathematics Exam &nbsp;·&nbsp; <strong id="tb-name">Student</strong></div>
    <div class="tb-timer" id="timer">30:00</div>
  </div>
  <div class="exam-wrap">
    <div class="q-dots" id="q-dots"></div>
    <div class="progress-row">
      <div class="prog-bar"><div class="prog-fill" id="prog-fill" style="width:5%"></div></div>
      <div class="prog-txt" id="prog-txt">1 / 20</div>
    </div>
    <div class="qcard" id="qcard"><!-- dynamic --></div>
    <div class="nav-row">
      <button class="btn-nav btn-prev" onclick="goQ(-1)">← Hore</button>
      <button class="btn-nav btn-next" id="btn-next" onclick="nextOrSubmit()">Xiga →</button>
    </div>
  </div>
</div>

<!-- ══════ RESULTS ══════ -->
<div class="screen" id="s-result">
  <div class="rcard">
    <div class="rhead">
      <h2>Natiijooyinka Imtixaanka</h2>
      <p id="r-name-line">Magaca Ardayga</p>
    </div>
    <div class="rbody">
      <div class="score-circle">
        <div><span class="sc-num" id="r-score">0</span><span class="sc-denom">/20</span></div>
        <div class="sc-label">GUUD AHAAN MARKADA</div>
        <div class="sc-grade" id="r-grade"></div>
      </div>
      <div class="stats-grid">
        <div class="sg-cell"><div class="sc-val" id="r-correct" style="font-size:20px;font-weight:700;color:#10b981"></div><div class="sg-lbl">Saxan</div></div>
        <div class="sg-cell"><div class="sc-val" id="r-wrong" style="font-size:20px;font-weight:700;color:#ef4444"></div><div class="sg-lbl">Khaldan</div></div>
        <div class="sg-cell"><div class="sc-val" id="r-pct" style="font-size:20px;font-weight:700;color:var(--navy)"></div><div class="sg-lbl">Boqolkiiba</div></div>
      </div>
      <div class="breakdown">
        <div class="bd-title">Faahfaahin Su'aalaha</div>
        <div id="bd-list"></div>
      </div>
      <button class="btn-redo" onclick="resetExam()">↺ &nbsp; Mar Kale Imtixaan Qaado</button>
    </div>
  </div>
  <div class="rcredit">Prepared by: <strong>Sacad Siciid</strong> &nbsp;·&nbsp; Jamhuriya University of Science & Technology</div>
</div>

<script>
// ══════════════════════════════════════════
//  QUESTION BANK  (20 questions, 3 topics)
// ══════════════════════════════════════════
const QS = [
  // ── ALGEBRAIC EXPRESSIONS ──
  {sec:"Algebraic Expressions",
   q:"Fududee: 4x + 3y − 2z + 5y − 3x + 4z",
   opts:["x + 8y + 2z","x + 8y − 2z","2x + 8y + 2z","x − 8y + 2z"],
   ans:0, exp:"4x−3x=x · 3y+5y=8y · −2z+4z=2z → x+8y+2z"},
  {sec:"Algebraic Expressions",
   q:"Koobi (factorize): 9st − 3sv − 6sw",
   opts:["3s(3t − v − 2w)","3(3st − sv − 2sw)","s(9t − 3v − 6w)","3s(t − v − w)"],
   ans:0, exp:"GCF = 3s · 9st÷3s=3t · 3sv÷3s=v · 6sw÷3s=2w → 3s(3t−v−2w)"},
  {sec:"Algebraic Expressions",
   q:"Ballaar: 3x(y − 2z)",
   opts:["3xy − 6xz","3xy + 6xz","3xy − 2xz","6xy − 3xz"],
   ans:0, exp:"3x·y=3xy · 3x·(−2z)=−6xz"},
  {sec:"Algebraic Expressions",
   q:"Ballaar oo fududee: 8(y − z) + 2(7x + z)",
   opts:["14x + 8y − 6z","14x + 8y + 6z","14x − 8y − 6z","14x + 8y − 2z"],
   ans:0, exp:"8y−8z+14x+2z = 14x+8y−6z"},
  {sec:"Algebraic Expressions",
   q:"Sharci xisaabeed ee Commutative waxay sheegtaa:",
   opts:["a + b = b + a","a + (b+c) = (a+b) + c","a(b+c) = ab + ac","a − b = b − a"],
   ans:0, exp:"Commutative Law: a+b = b+a"},
  {sec:"Algebraic Expressions",
   q:"Fududee: 4uv − 7uz − 6wz + 2uv + 3wz",
   opts:["6uv − 7uz − 3wz","6uv + 7uz − 3wz","2uv − 7uz − 3wz","6uv − 7uz + 3wz"],
   ans:0, exp:"4uv+2uv=6uv · −7uz · −6wz+3wz=−3wz"},
  // ── POWERS ──
  {sec:"Powers (Indices)",
   q:"Fududee: x⁷ ÷ x³",
   opts:["x⁴","x¹⁰","x³","x⁷"],
   ans:0, exp:"a^m ÷ a^n = a^(m−n) → x^(7−3) = x⁴"},
  {sec:"Powers (Indices)",
   q:"Xisaabi: 6⁻² = ?",
   opts:["1/36","−36","1/12","36"],
   ans:0, exp:"a^(−n) = 1/a^n → 6^(−2) = 1/6² = 1/36"},
  {sec:"Powers (Indices)",
   q:"Xisaabi: 8^(1/3) = ?",
   opts:["2","4","3","8"],
   ans:0, exp:"8^(1/3) = ∛8 = 2"},
  {sec:"Powers (Indices)",
   q:"Fududee: (x²)³",
   opts:["x⁶","x⁵","x²","x⁸"],
   ans:0, exp:"(a^m)^n = a^(m×n) → (x²)³ = x^(2×3) = x⁶"},
  {sec:"Powers (Indices)",
   q:"Xisaabi: 5⁶ ÷ 5²",
   opts:["5⁴","5³","5⁸","25"],
   ans:0, exp:"5^(6−2) = 5⁴ = 625"},
  {sec:"Powers (Indices)",
   q:"Wax udhig standard form: 423.8",
   opts:["4.238 × 10²","42.38 × 10¹","4.238 × 10³","0.4238 × 10³"],
   ans:0, exp:"Standard form: 4.238 × 10²"},
  {sec:"Powers (Indices)",
   q:"Xisaabi: 625^(1/4)",
   opts:["5","25","4","6.25"],
   ans:0, exp:"625^(1/4) = ⁴√625 = 5 (because 5⁴=625)"},
  // ── LOGARITHMS ──
  {sec:"Logarithms",
   q:"log₂ 16 = ?",
   opts:["4","2","8","16"],
   ans:0, exp:"2⁴ = 16 → log₂ 16 = 4"},
  {sec:"Logarithms",
   q:"Haddii 4 = logₓ 81, x = ?",
   opts:["3","4","9","81"],
   ans:0, exp:"x⁴ = 81 = 3⁴ → x = 3"},
  {sec:"Logarithms",
   q:"Sharci Logarithmka: log_a(MN) = ?",
   opts:["log_a M + log_a N","log_a M − log_a N","log_a M × log_a N","log_a(M+N)"],
   ans:0, exp:"Product Rule: log_a(MN) = log_a M + log_a N"},
  {sec:"Logarithms",
   q:"Fududee: log_a M³ = ?",
   opts:["3 log_a M","log_a 3M","3 + log_a M","(log_a M)³"],
   ans:0, exp:"Power Rule: log_a(M^n) = n·log_a M"},
  {sec:"Logarithms",
   q:"Sharci isbeddelka asalka: log_b x = ?",
   opts:["log x ÷ log b","log b ÷ log x","log(x−b)","log x × log b"],
   ans:0, exp:"Change of base: log_b x = log x / log b"},
  {sec:"Logarithms",
   q:"log₁₀ 1000 = ?",
   opts:["3","2","4","10"],
   ans:0, exp:"10³ = 1000 → log₁₀ 1000 = 3"},
  {sec:"Logarithms",
   q:"Xisaabi (2 log_a x) − log_a(x−1) = log_a(x−2). Haddii la fududeeyo, maxay la mid tahay?",
   opts:["log_a(x²/(x−1)) = log_a(x−2)","log_a(2x−(x−1))","log_a x² − log_a(x−2)","log_a(x+1)"],
   ans:0, exp:"2log_a x = log_a x² → log_a(x²/(x−1)) = log_a(x−2)"},
];

let cur=0, answers=Array(QS.length).fill(null), answered=Array(QS.length).fill(false);
let timerInt, secondsLeft=1800, studentName='';

// ── START ──
function startExam(){
  const n=document.getElementById('inp-name').value.trim();
  if(!n){document.getElementById('inp-name').focus();document.getElementById('inp-name').style.borderBottomColor='#ef4444';return;}
  studentName=n;
  document.getElementById('tb-name').textContent=n;
  show('s-exam');
  buildDots();
  renderQ();
  startTimer();
}

function show(id){
  document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
}

// ── TIMER ──
function startTimer(){
  timerInt=setInterval(()=>{
    secondsLeft--;
    const m=Math.floor(secondsLeft/60), s=secondsLeft%60;
    const el=document.getElementById('timer');
    el.textContent=String(m).padStart(2,'0')+':'+String(s).padStart(2,'0');
    if(secondsLeft<=300) el.classList.add('warn');
    if(secondsLeft<=0){clearInterval(timerInt);submitExam();}
  },1000);
}

// ── DOTS ──
function buildDots(){
  const c=document.getElementById('q-dots');
  c.innerHTML='';
  QS.forEach((_,i)=>{
    const d=document.createElement('div');
    d.className='qdot'+(i===0?' active':'');
    d.textContent=i+1;
    d.onclick=()=>jumpQ(i);
    c.appendChild(d);
  });
}
function updateDots(){
  document.querySelectorAll('.qdot').forEach((d,i)=>{
    d.className='qdot'+(answered[i]?' done':'')+(i===cur?' active':'');
  });
}

// ── RENDER Q ──
function renderQ(){
  const q=QS[cur];
  const chosen=answers[cur];
  const isAnswered=answered[cur];

  let optsHtml=q.opts.map((o,i)=>{
    let cls='opt';
    let tag='';
    if(isAnswered){
      if(i===q.ans){cls+=' correct';tag='<span class="feedback-tag c">✓ Saxan</span>';}
      else if(i===chosen){cls+=' wrong';tag='<span class="feedback-tag w">✗ Khaldan</span>';}
    } else if(i===chosen) cls+=' selected';
    return `<div class="${cls}" onclick="selectOpt(${i})">
      <div class="opt-key">${['A','B','C','D'][i]}</div>
      <div class="opt-text">${o}</div>${tag}
    </div>`;
  }).join('');

  document.getElementById('qcard').innerHTML=`
    <div class="qcard-head">
      <div class="q-badge">Su'aal ${cur+1}</div>
      <div class="q-section">${q.sec}</div>
      <div class="q-marks-tag">1 Mark</div>
    </div>
    <div class="qcard-body">
      <div class="q-text">${q.q}</div>
      <div class="options">${optsHtml}</div>
      ${isAnswered?`<div style="margin-top:14px;padding:10px 14px;background:#f0f4fb;border-left:3px solid var(--blue);font-size:12.5px;color:#555"><strong>Sharaxaad:</strong> ${q.exp}</div>`:''}
    </div>`;

  // progress
  const pct=Math.round((cur+1)/QS.length*100);
  document.getElementById('prog-fill').style.width=pct+'%';
  document.getElementById('prog-txt').textContent=`${cur+1} / ${QS.length}`;

  // next/submit btn
  const btn=document.getElementById('btn-next');
  if(cur===QS.length-1){btn.textContent='✔ Gudbi Imtixaanka';btn.className='btn-nav btn-submit';}
  else{btn.textContent='Xiga →';btn.className='btn-nav btn-next';}

  updateDots();
}

// ── SELECT ──
function selectOpt(i){
  if(answered[cur]) return;
  answers[cur]=i;
  answered[cur]=true;
  renderQ();
}

// ── NAV ──
function goQ(d){
  cur=Math.max(0,Math.min(QS.length-1,cur+d));
  renderQ();
}
function jumpQ(i){cur=i;renderQ();}
function nextOrSubmit(){
  if(cur===QS.length-1) submitExam();
  else goQ(1);
}

// ── SUBMIT ──
function submitExam(){
  clearInterval(timerInt);
  const correct=answers.reduce((n,a,i)=>n+(a===QS[i].ans?1:0),0);
  const wrong=QS.length-correct;
  const pct=Math.round(correct/QS.length*100);

  document.getElementById('r-name-line').textContent=studentName+' · Macalin: Sacad Siciid';
  document.getElementById('r-score').textContent=correct;
  document.getElementById('r-correct').textContent=correct;
  document.getElementById('r-wrong').textContent=wrong;
  document.getElementById('r-pct').textContent=pct+'%';

  let gc,gl;
  if(pct>=80){gc='grade-A';gl='A – Ku Fiican';}
  else if(pct>=65){gc='grade-B';gl='B – Wanaagsan';}
  else if(pct>=50){gc='grade-C';gl='C – Dhexdhexaad';}
  else{gc='grade-F';gl='F – Ku Noqo Wax Barasho';}
  const ge=document.getElementById('r-grade');
  ge.textContent=gl; ge.className='sc-grade '+gc;

  // breakdown
  const bdl=document.getElementById('bd-list');
  bdl.innerHTML=QS.map((q,i)=>{
    const ok=answers[i]===q.ans;
    const chosen=answers[i]!==null?q.opts[answers[i]]:'(Lama jawabin)';
    return `<div class="bd-item">
      <div class="bd-num">${i+1}</div>
      <div class="bd-q">${q.q.length>60?q.q.slice(0,60)+'…':q.q}
        <div class="bd-ans">${ok?'✓ '+q.opts[q.ans]:'Jawaabta saxda ah: '+q.opts[q.ans]+' · Adigu: '+chosen}</div>
      </div>
      <div class="bd-tag ${ok?'c':'w'}">${ok?'✓':'✗'}</div>
    </div>`;
  }).join('');

  show('s-result');
}

// ── RESET ──
function resetExam(){
  cur=0;
  answers=Array(QS.length).fill(null);
  answered=Array(QS.length).fill(false);
  secondsLeft=1800;
  document.getElementById('inp-name').value='';
  document.getElementById('inp-name').style.borderBottomColor='';
  document.getElementById('timer').classList.remove('warn');
  show('s-welcome');
}
</script>
</body>
      </html>
